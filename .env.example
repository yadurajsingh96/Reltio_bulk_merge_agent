# =============================================================================
# Reltio Bulk Merge Agent - Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env

# -----------------------------------------------------------------------------
# Reltio Connection (Required)
# -----------------------------------------------------------------------------
RELTIO_CLIENT_ID=your_client_id
RELTIO_CLIENT_SECRET=your_client_secret
RELTIO_TENANT_ID=your_tenant_id
RELTIO_ENVIRONMENT=dev            # dev | test | prod | prod-usg

# -----------------------------------------------------------------------------
# LLM Provider (Optional - enables AI-powered match analysis)
# Set ONE of the following. If both are set, OpenAI takes priority.
# -----------------------------------------------------------------------------
# OPENAI_API_KEY=sk-...
# ANTHROPIC_API_KEY=sk-ant-...

# -----------------------------------------------------------------------------
# MCP Server (Optional - for MCP-based integration)
# If set, the agent can also leverage Reltio MCP Server tools
# -----------------------------------------------------------------------------
# MCP_SERVER_URL=http://localhost:8000/sse

# -----------------------------------------------------------------------------
# Processing Options
# -----------------------------------------------------------------------------
# ENTITY_TYPE=HCP                 # Default entity type for matching
# MAX_CONCURRENT=10               # Max concurrent API requests
# AUTO_MERGE_THRESHOLD=95         # Score threshold for auto-merge (0-100)
# REVIEW_THRESHOLD=70             # Score threshold for review (0-100)

# -----------------------------------------------------------------------------
# SSL / Corporate CA Certificate (Optional)
# Required when behind a corporate proxy like Zscaler that performs TLS
# inspection and signs traffic with its own CA certificate.
#
# Option A – point to the CA bundle file (recommended):
#   Export your Zscaler (or other corporate) root CA as a .pem or .crt file,
#   then set the path here.  The file may contain multiple certificates.
#   SSL_CA_BUNDLE=/path/to/corporate-ca-bundle.pem
#
# Option B – disable SSL verification entirely (NOT recommended for production):
#   SSL_VERIFY=false
# -----------------------------------------------------------------------------
# SSL_CA_BUNDLE=/path/to/corporate-ca-bundle.pem
# SSL_VERIFY=true
